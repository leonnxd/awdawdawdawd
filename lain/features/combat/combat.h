#pragma once
#include "../../util/globals.h"
#include "../../util/classes/classes.h"

namespace combat {
    // No longer need get_autokill_target as autokill is removed.
    // Player status is handled via globals::bools::player_status.
}
